<div class='nested-fields'>
  <%= f.input :service, collection: SocialMediaLink::SERVICES.map{ |service| [service.camelize, service] } %>
  <%= f.input :handle %>
  <%= link_to_remove_association 'Remove Social Media Link', f, class: 'btn btn-default' %>
  <hr />
</div>
