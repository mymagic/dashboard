<ul class="nav nav-pills filter-navigation">
  <% %i(everyone members mentors staff).each do |filter| %>
    <%= content_tag 'li', role: 'presentation', class: (current_filter == filter ? 'active' : false) do %>
      <%= link_to filter.to_s.humanize, community_members_path(current_community, filter_by: filter) %>
    <% end %>
  <% end %>
</ul>
