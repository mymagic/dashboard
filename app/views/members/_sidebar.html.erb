<div class="col-lg-4">
  <div class="col-lg-12">
    <div class="thumbnail">
      <div class="member-thumb-wrapper">
        <%= image_tag(@member.avatar.url(:thumb)) %>
      </div>
    </div>
  </div>
  <% @member.positions_in_companies.each do |company, positions| %>
    <%= panel company.name, columns: 12 do %>
      <%= positions.map(&:name).to_sentence %>
    <% end %>
  <% end %>
</div>
