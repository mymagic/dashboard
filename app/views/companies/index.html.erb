<%
  title 'Portfolio'
  page_header "#{ current_community.name } Portfolio"
%>

<%= render(partial: 'filters',
           layout:  'layouts/filter_navigation',
           locals: { current_filter: @filter, filters: Company::FILTERS }) %>

<%= render(partial: 'shared/paginated_collection',
           locals: { objects: @companies, class_name: 'Company' }) %>
