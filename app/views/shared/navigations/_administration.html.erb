<nav class="navbar navbar-default navbar-static-top navbar-admin">
  <div class="container">
    <div class="navbar-header">
      <button aria-controls="navbar" aria-expanded="false" class="navbar-toggle collapsed" data-target="#navbar-admin" data-toggle="collapse" type="button">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <strong class='navbar-brand'>Administration</strong>
    </div>
    <div class="navbar-collapse collapse" id="navbar-admin">
      <ul class="nav navbar-nav">
        <%= nav_link "Settings", edit_community_admin_community_path(current_community) if can? :administrate, current_community  %>
        <%= nav_link "Networks", community_admin_networks_path(current_community) if can? :administrate, Network %>
        <%= nav_link "Companies", community_admin_companies_path(current_community) if can? :administrate, Company  %>
        <%= nav_link "Members", community_admin_members_path(current_community) if can? :administrate, Member %>
        <%= nav_link "Events", community_admin_events_path(current_community) if can? :administrate, Event %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            Visit Network<span class="caret"></span>
          </a>
          <ul class="dropdown-menu">
            <% current_community.networks.where.not(id: nil).each do |network| %>
              <li><%= link_to network.name, [network.community, network] %></li>
            <% end %>
          </ul>
        </li>
        <%= nav_link "Sign out", destroy_member_session_path(current_community), method: :delete %>
      </ul>
    </div>
  </div>
</nav>
