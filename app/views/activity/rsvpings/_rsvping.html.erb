<%= render layout: 'shared/activity', locals: { activity: rsvping } do %>
  is <%= rsvping.state.humanize(capitalize: false) %> <%= link_to rsvping.event_title, community_event_path(current_community, rsvping.event_id) %>
<% end %>
