<% filters.each do |filter| %>
  <%= content_tag 'li', role: 'presentation', class: (current_filter == filter ? 'active' : false) do %>
    <% if tag %>
      <%= link_to filter.to_s.humanize, community_discussion_tag_path(current_community, tag_id: tag, filter: filter) %>
    <% else %>
      <%= link_to filter.to_s.humanize, community_discussions_path(current_community, filter: filter) %>
    <% end %>
  <% end %>
<% end %>
