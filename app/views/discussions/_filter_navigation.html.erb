<ul class="nav nav-pills">
  <% %i(recent hot popular).each do |filter| %>
    <%= content_tag 'li', role: 'presentation', class: (current_filter == filter ? 'active' : false) do %>
      <%= link_to filter.to_s.humanize, community_discussions_path(current_community, filter_by: filter) %>
    <% end %>
  <% end %>
</ul>
