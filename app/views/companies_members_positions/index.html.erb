<%
  title "#{ @company.name } Members Positions"
  page_header "#{ @company.name } Members Positions"
%>

<div class="row">
  <div class="col-sm-12" role="tabpanel">
    <ul class="nav nav-tabs" role="tablist">
      <li class="active" role="presentation">
        <a aria-controls="approved" data-toggle="tab" href="#approved" role="tab">
          Approved
          <span class="label label-info"><%= @approved_companies_members_positions.size %></span>
        </a>
      </li>
      <% if @pending_companies_members_positions.any? %>
        <li role="presentation">
          <a aria-controls="pending" data-toggle="tab" href="#pending" role="tab">
            pending
            <span class="label label-info"><%= @pending_companies_members_positions.size %></span>
          </a>
        </li>
      <% end %>
    </ul>
    <div class="tab-content">
      <div class="tab-pane active fade in" id="approved" role="tabpanel">
        <table class="table table-striped">
          <thead>
            <th>Position</th>
            <th></th>
            <th>Company</th>
            <th></th>
            <th>Member</th>
            <th></th>
          </thead>
          <tbody>
            <%= render collection: @approved_companies_members_positions, partial: 'approved_companies_members_position', as: :companies_members_position %>
          </tbody>
        </table>
      </div>
      <div class="tab-pane fade" id="pending" role="tabpanel">
        <table class="table table-striped">
          <thead>
            <th>Position</th>
            <th></th>
            <th>Company</th>
            <th></th>
            <th>Member</th>
            <th></th>
          </thead>
          <tbody>
            <%= render collection: @pending_companies_members_positions, partial: 'pending_companies_members_position', as: :companies_members_position %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
