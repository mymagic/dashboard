<%
  title @event.title
  page_header event_header(@event) %>

<div class="row">
  <div class="col-sm-8 col-xs-8">
    <%= @event.description %>
  </div>
  <div class="col-sm-4 col-xs-4">
    <%= render partial: 'location', locals: { event: @event } %>
    <%= render partial: 'time', locals: { event: @event } %>
  </div>
</div>
